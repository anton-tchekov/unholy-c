fn main()
{
	var board, w, h;

	board = 0x8000;
	w = 40;
	h = 20;
println("before!");

	init(board, w, h);

println("Printing board!");
}

fn init(board, w, h)
{
	println("wtf?");
	/*var end;

	end = add(board, mul(w, h));
	while gt(end, board)
	{
		end = sub(end, 1);
		w8(end, ' ');
	}*/
}
